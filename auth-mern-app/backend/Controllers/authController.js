const user = require('../Models/user');

module.exports.signupControl = (req,res) => {
    res.send('signup successfull')
}
module.exports.loginControl = (req,res) => {
    res.send('login successfull')
}